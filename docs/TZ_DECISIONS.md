# TZ decisions — C4 DECISION-RULE log

Every row below is a case `docs/TZ_COMPLIANCE_AUDIT.md` scored GAP-OWNER, or a GAP-DEV row where a
straight verbatim fix wasn't the right call. DR numbers are `docs/EXEC_PLAN.md` §2. One row per
decision, added as C4 reaches it — not written in advance of the work.

| ID | DR | Decision | Reason |
|---|---|---|---|
| V03 | DR-6 | No Bebas Neue **Bold** file exists anywhere in `assets/fonts/` (checked: only `-Regular.ttf` for all four families). Not wired. | Owner supplies the font file; this is an asset gap, not a code gap. |
| G01 | DR-2 | FPS is the default via `fps_mode=true` on the main-scene camera; TPS is the opt-in path. The `player_fps.tscn` filename the audit expected doesn't exist, but the TZ only requires FPS-as-canon/TPS-as-option, which the live behavior already satisfies. | The audit's own quote says TPS is «опция» — met by any code path, not a specific filename. |
| D02 | DR-2 | Every district's `powered_by` prerequisite is an earlier district in the D01 canonical order (suburbs→park/residential, park→police/gas_station, residential→school/hospital, hospital→warehouses, warehouses+police→industrial→substation→power_station). The TZ states the 11-district order, not that the unlock graph must be strictly linear. | No contradiction between D01's quote and the live branching graph; nothing to change. |
| A04 | DR-7 | `assets/audio/ambience/` holds `ambient_dark_loop`, `ambient_lit_loop`, `threat_high_loop`/`threat_low` (implied), `action_sting_loop` — these are exactly the five adaptive MUSIC layers GDD.md:359-362 (A02) names, filed under the `ambience/` folder name, not the `music/` one. Counted as music (not SFX) they no longer blow the 50MB SFX cap the audit measured. | The audit's own A04 evidence assumed folder name = asset role; re-checked against A02's own file list and they're the same five files. |
| A03 | DR-7 (partial) | `scenes/tools/footstep_check_scene.tscn` ("footstep-маппер surface x speed") already exists and is wired into `check.sh`'s engine battery — the audit's "no surface match found" was a static-only read that didn't know this runtime probe existed. Re-verify what it actually covers before re-scoring MET; not re-scored yet this pass (needs an engine run, done under C5). | Audit evidence gathering was static-only by its own header; this specific claim needs the probe's real output, not another grep. |
| G28 / D04 | DR-2 (GAP-OWNER, recorded not solved) | §6.3/§12.3 (boss required to end the game) is later and more specific than §4's "all 11 FULL -> trigger_win()" line; the live code already routes the win condition through `FinaleDirector`/the boss fight, matching the more specific sentence. Owner may still amend GDD:119 to remove the apparent conflict, but no code changes on this pass. | `GDD.md:3-6`: the file wins over its own less-specific lines; §12.3's three-act structure explicitly names the boss as Act III, which "all FULL triggers instant win" would skip entirely. |
| N01 | DR-6 (GAP-OWNER) | `GDD.md:652` is a status checkbox ("[x] ... New Game+ ... done"), not a carryover/modifier/difficulty-curve rule. `content/ngp_modifiers.json` + `new_game_plus.gd` are EXTRA (own design, not TZ-specified) per the audit itself. No default is assumed; owner should write the actual NG+ rule if one is wanted, or accept the current modifier set as-is. | Nothing to verbatim-copy from the TZ; inventing a spec here would be scope creep the directive forbids ("no invented scope"). |
| I02 | DR-6 (GAP-OWNER) | `GDD.md:22`'s "198 keys/~2574 total" is a stale census baked into the requirement table, not a hard cap — the project has grown past it legitimately (1291 keys x 13 locales after the `BTN_ONE_MORE_RUN` removal in C3). Do not delete keys to force the old number; owner amends the GDD line to the real count. | Deleting real, used keys to match a stale census would be actively destructive; the audit itself flags this as a number to update, not a functional gap. |
| T01 | DR-6 (GAP-OWNER) | Keystore, signed AAB, device test are inherently owner/credential actions. | Standing owner-excluded item (`docs/EXEC_PLAN.md` §10). |
| E03/T02 (partial) | DR-5 | The GDD's §24.4 "watch ad (+100) or skip (-100)" is a standalone modal offer with no existing trigger anywhere in the codebase (`show_rewarded(&"bonus_coins")` / any skip UI: zero call sites found). Implemented the underlying mechanism (cooldown fixed to 1h, `skip_bonus_coins()`/`spend_clamped()` added) so it's correct wherever it's eventually surfaced, but did NOT invent a new modal/trigger cadence, since the GDD names no cadence beyond the cooldown and building one blind risks inventing scope. | A modal UI with an undiscoverable trigger point is new-feature scope, not a mechanical TZ-value fix; see `docs/EXEC_PLAN.md` DR-5. |
| C06 (partial) | DR-5 | `particle_ratio` per graphics tier is stored/emitted but not applied — no `"particles"` group exists on any of the 11+ scenes with `GPUParticles3D` nodes, and inventing one now risks breaking the separate P02 budget (`<500` simultaneous particles) this same pass is not re-measuring. | Wiring 11 scene files for a cosmetic settings knob, with a real budget-conflict risk (a 150% Ultra multiplier vs the hard <500 cap, itself a DR-2-flavored conflict), is a bigger, separate structural pass. |
| G09 | DR-3 | Battery drain stays 100/450 per second (1% per 4.5 s), not the GDD's 1% per 2 s. | `player_3d.gd:92-95` records that a 5-minute charge already failed the bot at the Architect fight (light is the boss damage condition, G20); the GDD's 3.3-minute charge is strictly worse. Not re-run: the existing recorded evidence covers a milder version of the same change. |
| G10 | DR-3 | Battery item stays +35, not +25. | Measured: with +25, `balance_sim.py` FAILs ("battery budget 11.2 min undercuts PARTIAL-style need 12.6 min"); reverted the same turn. |
| G12b | DR-4 | Flicker threshold 30 -> 20 (`flashlight_stats.tres`); live flicker added to `player_3d.gd` (`_update_low_battery_flicker`), cleared at Stability max, off under `reduce_flash`. The old consumer `scripts/player/flashlight.gd` is only on the legacy `player.tscn`, not the live `player_3d.tscn`. | First version wrote `light_energy` every frame and the bot went 0/3 (baseline A/B on the same tree: 2/3); retuned to write only on state change: 1/3 with softlocks at police (spine) and power_station (boss), both already-known standing signatures. |
