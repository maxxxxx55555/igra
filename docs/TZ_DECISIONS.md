# TZ decisions — C4 DECISION-RULE log

Every row below is a case `docs/TZ_COMPLIANCE_AUDIT.md` scored GAP-OWNER, or a GAP-DEV row where a
straight verbatim fix wasn't the right call. DR numbers are `docs/EXEC_PLAN.md` §2. One row per
decision, added as C4 reaches it — not written in advance of the work.

| ID | DR | Decision | Reason |
|---|---|---|---|
| V03 | DR-6 | No Bebas Neue **Bold** file exists anywhere in `assets/fonts/` (checked: only `-Regular.ttf` for all four families). Not wired. | Owner supplies the font file; this is an asset gap, not a code gap. |
| G01 | DR-2 | FPS is the default via `fps_mode=true` on the main-scene camera; TPS is the opt-in path. The `player_fps.tscn` filename the audit expected doesn't exist, but the TZ only requires FPS-as-canon/TPS-as-option, which the live behavior already satisfies. | The audit's own quote says TPS is «опция» — met by any code path, not a specific filename. |
| D02 | DR-2 | Every district's `powered_by` prerequisite is an earlier district in the D01 canonical order (suburbs→park/residential, park→police/gas_station, residential→school/hospital, hospital→warehouses, warehouses+police→industrial→substation→power_station). The TZ states the 11-district order, not that the unlock graph must be strictly linear. | No contradiction between D01's quote and the live branching graph; nothing to change. |
| A04 | DR-7 (reason corrected in C8 round 3; see CORRECTION_LOG #23) | The caps hold for the exported build: `ambience/wav_src` (29.9 MB of source WAVs) is in `exclude_filter`. Exported non-music is about 23.5 MB, music about 38 MB. The original reason below was wrong: `ambience/` also holds 15 MB of real district, detail and weather ambience, and the dark/lit layers live in `music/`. Original: `assets/audio/ambience/` holds `ambient_dark_loop`, `ambient_lit_loop`, `threat_high_loop`/`threat_low` (implied), `action_sting_loop` — these are exactly the five adaptive MUSIC layers GDD.md:359-362 (A02) names, filed under the `ambience/` folder name, not the `music/` one. Counted as music (not SFX) they no longer blow the 50MB SFX cap the audit measured. | The audit's own A04 evidence assumed folder name = asset role; re-checked against A02's own file list and they're the same five files. |
| A03 | DR-7 (partial) | `scenes/tools/footstep_check_scene.tscn` ("footstep-маппер surface x speed") already exists and is wired into `check.sh`'s engine battery — the audit's "no surface match found" was a static-only read that didn't know this runtime probe existed. Re-verify what it actually covers before re-scoring MET; not re-scored yet this pass (needs an engine run, done under C5). | Audit evidence gathering was static-only by its own header; this specific claim needs the probe's real output, not another grep. |
| G28 / D04 | DR-2 (GAP-OWNER, recorded not solved) | §6.3/§12.3 (boss required to end the game) is later and more specific than §4's "all 11 FULL -> trigger_win()" line; the live code already routes the win condition through `FinaleDirector`/the boss fight, matching the more specific sentence. Owner may still amend GDD:119 to remove the apparent conflict, but no code changes on this pass. | The GDD has no precedence rule between its own sections (`GDD.md:3-7` only puts it above its appendices), so this is a judgement: §12.3's three-act structure explicitly names the boss as Act III, which "all FULL triggers instant win" would skip entirely. Owner call. |
| N01 | DR-6 (GAP-OWNER) | `GDD.md:652` is a status checkbox ("[x] ... New Game+ ... done"), not a carryover/modifier/difficulty-curve rule. `content/ngp_modifiers.json` + `new_game_plus.gd` are EXTRA (own design, not TZ-specified) per the audit itself. No default is assumed; owner should write the actual NG+ rule if one is wanted, or accept the current modifier set as-is. | Nothing to verbatim-copy from the TZ; inventing a spec here would be scope creep the directive forbids ("no invented scope"). |
| I02 | DR-6 (GAP-OWNER) | `GDD.md:22`'s "198 keys/~2574 total" is a stale census baked into the requirement table, not a hard cap — the project has grown past it legitimately (1301 keys x 13 locales at rc10, counted from `data/i18n/*.json`). Do not delete keys to force the old number; owner amends the GDD line to the real count. | Deleting real, used keys to match a stale census would be actively destructive; the audit itself flags this as a number to update, not a functional gap. |
| T01 | DR-6 (GAP-OWNER) | Keystore, signed AAB, device test are inherently owner/credential actions. | Standing owner-excluded item (`docs/EXEC_PLAN.md` §10). |
| E03/T02 (partial) | DR-5 | The GDD's §24.4 "watch ad (+100) or skip (-100)" is a standalone modal offer with no existing trigger anywhere in the codebase (`show_rewarded(&"bonus_coins")` / any skip UI: zero call sites found). Implemented the underlying mechanism (cooldown fixed to 1h, `skip_bonus_coins()`/`spend_clamped()` added) so it's correct wherever it's eventually surfaced, but did NOT invent a new modal/trigger cadence, since the GDD names no cadence beyond the cooldown and building one blind risks inventing scope. | A modal UI with an undiscoverable trigger point is new-feature scope, not a mechanical TZ-value fix; see `docs/EXEC_PLAN.md` DR-5. |
| C06 (partial, SUPERSEDED: particles applied per tier since C4 (`settings_manager.gd` `_scale_emitter`, tz_verify 6/6 emitters at 150%); tier fog applied since `2547fff` but overridden on every load by `fog_setup.gd` until `b8b20c0`, CORRECTION_LOG #24) | DR-5 | Original C4-start note: `particle_ratio` per graphics tier was stored/emitted but not applied — no `"particles"` group exists on any of the 11+ scenes with `GPUParticles3D` nodes, and inventing one now risks breaking the separate P02 budget (`<500` simultaneous particles) this same pass is not re-measuring. | Wiring 11 scene files for a cosmetic settings knob, with a real budget-conflict risk (a 150% Ultra multiplier vs the hard <500 cap, itself a DR-2-flavored conflict), is a bigger, separate structural pass. |
| G09 | DR-3 | Battery drain stays 100/450 per second (1% per 4.5 s), not the GDD's 1% per 2 s. | `player_3d.gd:92-95` records that a 5-minute charge already failed the bot at the Architect fight (light is the boss damage condition, G20); the GDD's 3.3-minute charge is strictly worse. Not re-run: the existing recorded evidence covers a milder version of the same change. |
| G10 | DR-3 | Battery item stays +35, not +25. | Measured: with +25, `balance_sim.py` FAILs ("battery budget 11.2 min undercuts PARTIAL-style need 12.6 min"); reverted the same turn. |
| G12b | DR-4 | Flicker threshold 30 -> 20 (`flashlight_stats.tres`); live flicker added to `player_3d.gd` (`_update_low_battery_flicker`), cleared at Stability max, off under `reduce_flash`. The old consumer `scripts/player/flashlight.gd` is only on the legacy `player.tscn`, not the live `player_3d.tscn`. | First version wrote `light_energy` every frame and the bot went 0/3 (baseline A/B on the same tree: 2/3); retuned to write only on state change: 1/3 with softlocks at police (spine) and power_station (boss), both already-known standing signatures. |
| G06 | DR-4 | `run_speed` 300 -> 272 (= walk 170 x 1.6, GDD.md:68). | Bot 3/3 wins, 0 softlocks. |
| G07 (partial) | DR-4 | Crouch noise now walk-noise x `CROUCH_NOISE_MULT` (0.3), previously an unread constant. Crouch speed x0.4 was already applied. NOT done: visibility x0.5 (no consumer path traced), 1.2 m crouch capsule (needs an uncrouch ceiling check to avoid getting stuck; DEFERRED-STRUCTURAL). | Same bot run. |
| G08 (partial) | DR-4 | Flashlight colour -> `#c9a24a`, cone angle 35 -> 45 (GDD.md:76). NOT changed: range 16 m (GDD 8 m) and energy 24 (GDD 2.0): both are tuned for the gl_compatibility renderer and the range feeds `refresh_flashlight_range()`; halving it is a real gameplay/visibility change and the energy number may be a renderer-unit mismatch. No DR-1..3 evidence exists for keeping either, so both are NEEDS-MEASUREMENT, not NEEDS-EYES (the legend reserves that for subjective look): run the guarded windowed G08 frame and a 3-seed IRON RULE bot at 8 m / 2.0 against 16 m / 24; if 8 m / 2.0 loses bot wins or the frame shows no readable pool, record DR-3 with that log and frame, otherwise apply DR-4 (the upgrade formulas scale from the scene base, `player_3d.gd:239-247`). | Bot 2/3 wins; the one softlock is the suburbs spine stall already seen on unmodified HEAD (baseline seed 2). |
| G13 / G15 | DR-3 | Combo damage stays 14/21/35 and attack box stays 1.4x0.8x3.4 (the capsule half of G15 was applied separately: 1.6 m, bot 1/3, all seeds reached the boss). | `player_3d.gd:75-77` records the damage was raised ~x1.75 on purpose because the bot could not finish the Architect inside its deadline; `player_3d.gd:307-310` records that the old 0.6 m box barely reached the boss capsule and the fight ran into the timeout. GDD 8/12/20 and a 0.6x0.4x1.2 box shrink DPS/reach further. Not re-run for the same reason as G09. |
| V05 (correction) | DR-4 | Stays 2048 (GDD.md:317). An in-session A/B briefly blamed it for the magenta; that A/B was confounded by the texture import state. Re-run with the state held constant: 2048 and 1024 both 0.03% hue-magenta. | `docs/CORRECTION_LOG.md` #4 |
| E03 modal | BY-DESIGN-ABSENT | GDD §24.4 describes the watch(+100)/skip(-100) modal but cites no trigger, and the GDD never mentions interstitials. No trigger invented (no speculative UI). The cited half, "1 ad per hour", now applies to the existing non-canon district-entry interstitial too (180 s -> 3600 s). | GDD.md:613-616 |
| G16 | DR-4 | Death screen Retry -> `GameManager.respawn_after_death()`: load save, district entry (mid-fight autosave position dropped), HP 50%, battery as at death. No save -> new game (Hardcore). | Was `start_new_game()`: every death wiped the run. Suite P2r; bot 1/3 WIN, known stall types only. |
| G04 | DR-1 | Interaction stays proximity + facing cone, reach 3.2 m (GDD: 3 m camera ray). | A strict ray or shorter reach is a pickup/interaction tightening - the REJECTED list (PICKUP_TOUCH tightening). |
| G27 | DR-2 | Touch: left 35% = movement joystick, rest = look. | GDD.md:52-59 table gives the left half to camera drag and lists no movement input at all; applying it verbatim leaves touch players unable to walk. Table is incomplete, current behavior kept. |
| D03 / V01 | DR-4 VERBATIM (C8 rc12; was mislabelled DR-3) | Stage lighting now uses the GDD values: ambient 0.03/0.11/0.16 and moon 0.12/0.25/0.40 for DARK/STREETS/FULL (`world_env_setup.gd`). V01's latent daytime painter in `DayNight` removed (DR-4: GDD.md:261 "Дня нет"). | The old DR-3 cited a rejection of 0.01/0.03, not of the GDD values, so nothing measured rejected canon. tz_verify asserts all three stages and `D03_stage_*.png` were read by eye: DARK shows silhouettes, road edges and the tree as GDD 4.2 describes, FULL a lit street; all three pass `visual_truth_gate` at half res (0.24-0.46%, black 0%). |
| G31 / G32 / G33 | DR-2 | Endings keep the current mapping: 50-99% docs = Hope (TZ defines only <50%); "only D11" and "unrepaired grid" are reachable only through death (D02's unlock graph makes an alive D11-only state impossible). | Undefined or unreachable TZ cases keep current behavior. |
| G34 | DR-4 | Bunker = the real bunker secret `secret_power_station_02` (was aliased to "power station FULL", which every win already has). Audio logs and photos need no separate counters: the 22 `audio_log` and 24 `photo` lore notes (`content/districts/*/lore_notes.json`, spawned from `DistrictLoot.LORE_DOCS`) are part of `Endings.get_total_documents()`, so "all documents" already requires every one of them. | The earlier DR-5 premise ("content absent") was false for the lore notes (C8 round 11). Photo-mode snapshots (G26) are a separate feature and are not required. |
| S01 | DR-3 (measured) | Hit/dodge noise NOT applied. | IRON RULE bisect 2026-09-25: batch with S01 0/3 wins, same batch without S01 2/3 (HEAD alone 1/3). Monsters investigating every swing/dodge stalled the spine. Reverted; the implementation is recorded here for a future rebalance: 1.0/5 m on a landed hit, 0.4/3 m on a dodge, fading over ~0.5 s via get_noise_level(). |
| G18 / G19 | DR-7 | Re-scored MET-STATIC: HP/damage of all 11 types + boss equal the GDD.md:168-181 table (script check 2026-09-25); Shadow's stats (30/15, vision 0) live in `shadow_3d.gd`, not a roster row. The audit read the internal alias keys (`watcher`->`sniper` etc.) as wrong stats. | Values verified, not renamed (renaming keys touches saves and scenes for no player-visible gain). |
| G20 | DR-4 | Boss phases at 70% / 30% (was 66% / 33%), falling beams 40 (was 25; the player-side 12-per-hit cap still applies). P2 already summons up to 3 Shadows. | IRON RULE batch run. |
| G24 | DR-2 via criterion 2 | Point of no return at D10 closes only once D1-D9 are all FULL; before that, travel back stays open, with a localized notice when blocked. | The finale needs all 11 FULL (`_check_victory`), so a literal gate at first entry would strand a player with an unfinished district behind them: a softlock. Suite P2q asserts both sides. |
| S04 | DR-4 | Search after losing the player: 10 s, wandering within 5 m of the last known point (was 5 s standing on it). | IRON RULE batch run; suite P2q locks the constants. |
| S02 / G07-visibility | DEFERRED-STRUCTURAL | Monsters read one visibility factor only: the low_profile skill shortens their sight range while the player sneaks with the flashlight off (`base_monster.gd` `_can_see_player`); otherwise range + cone + line of sight. The GDD modifiers (flashlight x2, darkness 3 m, run +20%, crouch x0.5) are a detection-model rework needing a light-exposure query and a stealth rebalance; the bot always runs with the flashlight on, so x2 range shifts winnability broadly. | "Behind a wall = 0%" is already met through line of sight. |
| G21 | DEFERRED-STRUCTURAL | The five §9 blueprints are new mechanics (capacity upgrades, a UV damage cone, a field workbench) plus blueprint pickups at five named locations, not recipe rows. | New feature scope with behavior change. |
| G22 | DR-6 OWNER-ONLY (C8 rc12; was DR-3) | 3 manual + 1 auto slot picker stays archived. | PLAN.md §В "Этап 1 — decisions needed" asked the owner and records "DECIDED: archive" for the save-slot screens; that is an owner decision, not a measured rejection. Owner action: re-enable the slot picker or amend GDD G22. C2's slot_id binding assumes the archive. |
| G25 | DEFERRED-STRUCTURAL | HUD slots cannot hold "weapon x2": `weapon_manager.gd` is not instanced by any scene, so there is no live weapon to bind. Battery and medkit slots already match. | Wiring the weapon system into the player is its own feature. |
| G26 | DR-5 | No photo can be collected in play: `take_photo()` has no caller, the album total is hard-coded 200, and no photo spots exist. Photographer/Seeker/Collector achievements would be unreachable, so none were added. | Needs photo-spot content. |
| A01 | DEFERRED-STRUCTURAL | Bus graph `SFX (Footsteps, Combat, UI, Environment)` needs every stream re-routed and the mix re-balanced by ear; audio checks here are peak-only. | Current buses: Master, Music, SFX, Voice, Ambient, UI, Hum. |
| G07-capsule | DEFERRED-STRUCTURAL | A 1.2 m crouch capsule needs an uncrouch ceiling check, or the player can stand up inside geometry. | Crouch speed x0.4 and noise x0.3 are applied. |
| E05 | DR-4 VERBATIM (C8 rc12; was DR-3 with no measured rejection) | District reward escalates 200 + 100 per district in `DistrictManager.DISTRICTS` order (D1 200, D11 1200). | Measured first: a winning bot run earned 3439 coins by D11 on the flat 200 (`.qa_logs/c8_rc12_coins_s2.log`), against the GDD's 8000+. GDD.md:221/229 fix no district reward, so the escalation contradicts nothing. After the change the same kind of run earned 8718-8975. IRON RULE: see ORDER_PASS_REPORT round 11. |
| A03 | DR-5 SCOPE-CEILING (replaces the DR-7 "re-scored MET", which the C8 verifier found FAKE) | The three audible states STEALTH/WALK/RUN use the delivered walk/jog/sprint files (jog had been unused and stealth shared walk's sample). Asphalt, puddle and glass have one recorded sample each; speed is carried by volume (x0.3/1.0/1.5) and pitch (x0.9/1.0/1.12). | The wiring existing assets support is done; distinct per-speed recordings for asphalt, puddle and glass do not exist in-tree (residual: owner audio asset). Not counted MET. The probe now exits 1 if any surface has fewer than 3 distinct steps. |
| C03 | DEFERRED-STRUCTURAL (C8 re-score) | Auto-aim stays in `WeaponBase` only. | No gameplay path creates a weapon (G25 is DEFERRED-STRUCTURAL). The live melee hitbox includes a 2.7 m sphere that hits regardless of facing, so a melee aim-assist would change nothing measurable. |
| S03 | Note (no DR case: S03 is MET) | Ember vignette tint kept at canon #b4452f. | Alpha-blending ember over the moonlit blue pavement lands inside `visual_truth_gate`'s 260–345° hue band (median hue of the hits 319–335° on the cited frames). 70–98% of those pixels sit in the outer 15% edge band (the vignette): rc2 G03 87%, rc2 S03 98%, rc4 G03 85%, rc4 S03 70%, rc12 G03 98%, rc12 S03 88%. The threshold is not changed. The regression lock for R0 is the LUT import pin (`check.sh`, every `lut_*.png.import` must be `3d_texture`), not the visual gate. The visual gate's blocking run re-measures the `r0_after_*` frames from `24116c4`, which CORRECTION_LOG #1 shows were clean only because textures never loaded, so on its own it cannot catch an R0 regression. |
| P01 | DEFERRED-STRUCTURAL | D1 draw-call target (< 200) not met: windowed `perf_check_scene` measured 246 (C7) and 253 (rc11) on the real GPU; D11 (< 350) holds. | Getting under 200 needs material/mesh batching across the district scenes, not a value swap; CORRECTION_LOG #13 corrects the earlier MET-STATIC. |
| P02 | NEEDS-MEASUREMENT | Particle count < 500 and RAM/VRAM budgets are not re-measured. | Needs a windowed or on-device profile; the headless gates run on a dummy renderer that reports neither. |
| S04-hide | DEFERRED-STRUCTURAL | Hiding spots (lockers, bushes, car trunks, dark corners) are not placed in any district. `scripts/gameplay/hiding_spot.gd` (locker/dumpster/car/crate) exists and is kept as a planned feature, but nothing spawns it. | Placing hiding spots, and adding bush and dark-corner types, is level-design content with a stealth-balance change (IRON RULE). It is not a value fix. |
| V05-mobile | SUPERSEDED (C8 rc11, DR-4 VERBATIM) | The mobile 1024 override was removed; mobile now uses the GDD's 2048 too. | `GDD.md:317` has no mobile exception, and the earlier DR-3 label had no measurement behind it (the only A/B, on desktop, showed 1024 and 2048 equal), so canon wins. On-device cost is tracked under P02 (NEEDS-MEASUREMENT). |
