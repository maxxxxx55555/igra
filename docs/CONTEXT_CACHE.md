# Context cache — skills/refs activation

Checked `.claude/skills/*` (project) and `C:\Users\Maxsim\.claude\skills\*` (user)
at session start of the final-perfection-wave pass.

- Project skills: `ponytail`, `ponytail-audit`, `ponytail-debt`, `ponytail-gain`,
  `ponytail-help`, `ponytail-review` — the ponytail family, already the
  documented default mode for this repo (see root `CLAUDE.md`: "Default
  mode: ponytail... Shortest working diff, reuse before writing, delete
  over add, no unrequested abstractions"). No new rules beyond what's
  already governing this session.
- User-level `.claude/skills/` does not exist on this machine — nothing to
  cache from there.

No additional cacheable rules found; this file exists to record that the
check was done, per the session's "activate skills/refs" step.
