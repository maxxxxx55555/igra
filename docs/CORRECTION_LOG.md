# Correction log

Earlier claims later contradicted by evidence. Oldest first. Each row: what was claimed, what is
actually true, and the commit that establishes it.

| # | Old claim (where) | New fact | Commit |
|---|---|---|---|
| 1 | R0 magenta rendering corruption "fixed" by `scaling_3d/scale` 0.8→1.0, world magenta 2.46%→0.09% (RUN_STATE, `24116c4`; evidence frames `docs/stills/evidence/r0_after_*.png`) | Those frames were clean because the world textures **never loaded**: the committed `.import` files pointed at cache files this Godot build does not produce. With textures actually loaded the world is 13% magenta with banding rings. Real cause: the 11 district LUTs were imported as `CompressedTexture2D`, which `Environment.adjustment_color_correction` samples as a 1D gradient. Imported as Texture3D: 0.01–0.30% hue-magenta across 10 windowed frames. Lossless and S3TC were A/B'd and ruled out. | `bafb740` |
| 2 | "Stale headless import cache" as a recurring environment hazard, fixed by a windowed reimport (RUN_STATE Session 10, `fa5fee4`) | The recurring breakage came from **reverting `.import` files after the reimport**, which re-pointed them at nonexistent cache files. The fix is to commit the `.import` files Godot 4.7 actually writes. | `37581d7` |
| 3 | C06 graphics-tier fog MET (`f738e99`, TZ_COMPLIANCE) | The `SettingsManager` fog write was overwritten by `world_env_setup.gd`, and the windowed verifier measured 0.013 on every tier. Rebuilt on `visual_quality.tres` as the single source. | `2547fff` |
| 4 | V05 shadow atlas 2048 causes the magenta (said in-session on 2026-09-25 after one A/B, never committed as a decision) | Confounded: that A/B also swapped the texture import state. Re-run with import state held constant: 2048 = 0.03%, same as 1024. V05 stays 2048. | `bafb740` (method), this log |
| 5 | FUNCTION_MATRIX X22 "death screen doesn't display when boot is bypassed" = game bug | Test bug. Phase 8 read the legacy `Screens` node, whose autoload is commented out. The live death screen is opened by `UIManager`. | `0d3d533` |
| 6 | FUNCTION_MATRIX X20 "park-travel inf-position softlock" | `ppos=(inf…)` is the heartbeat sentinel for "no valid player", and the game was in PAUSED, which only a real Escape key reaches. The bot had no PAUSED branch. Harness fixed and A/B-proven; the keypress trigger is inferred, not proven. | `0d3d533` |
| 7 | FUNCTION_MATRIX footer: 58 WORKS / 43 UNTESTED | A recount of the rows gave 61 / 40. The footer was stale. The matrix now has 0 UNTESTED rows. | `0d3d533` |
| 8 | GOLD MASTER suite "exits 124 with no FAIL lines when clean" (C3 close-out) | Phases P2l, P2m and P3 fail intermittently on identical code (0 and 3 fails on repeated runs of the same HEAD). Root-caused and fixed: P2l/P3 were victims of the progress-signature bug (`ac877a5`); P2m landed in the gate scene's PLAYING->MENU window (bounded PLAYING wait, `9fc8665`; this row first cited the docs-only `c00f118`, see #21). Suite now `fails=0` on 3 consecutive runs. | `ac877a5`, `9fc8665` |
| 9 | TZ_COMPLIANCE_AUDIT A04: SFX over the 50 MB cap | The `ambience/` folder holds the five adaptive **music** layers GDD A02 names. Counted by role, not by folder, both caps hold. | `4bb5772` |
| 10 | Commit `d06fe48` message says "item 6" | It closed SLOP item 7 (the disabled-button StyleBox). The message is cosmetic, and the history is left as-is. | this log |
| 11 | Commit `ad051fc` message: tz_verify frames "all PASS visual_truth_gate" | False: V02_energy_ball failed at 0.59% hue-magenta (the ember light spill pushed pink by the LUT). Fixed with a brass light. | `ae410b9` |
| 12 | Commit `ae410b9` message: the one half-res FAIL is "C06 ultra tier" | It is `S03_noise_vignette.png` (0.66%): the saturation-outlier sub-detector counting the saturated ember edge vignette. | this log |
| 13 | TZ_COMPLIANCE P01 "MET-STATIC" (drawcall_estimate PASS) | Real GPU, windowed `perf_check_scene`: D1 suburbs **246** draw calls. The D11 < 350 budget passes; D1 < 200 does not. | this log |
| 14 | "S01 hit/dodge noise MET" (session notes before the IRON RULE bisect) | Not shipped: bot 0/3 with S01 vs 2/3 without. Recorded as DR-3 in TZ_DECISIONS. | `c00f118` |
| 15 | ARENA_CLOSURE R0 / ORDER_PASS_REPORT: tzverify frames "≤0.30%" magenta | That was the hue-only metric at full res. `visual_truth_gate` ORs a saturation-outlier detector and reads 0.17–0.66% on the rc1 frames (C8 verifier), with S03 over the 0.5% threshold. rc2 frames: 0.17–0.46%, and G03 at 1.01% (ember vignette, now actually visible). | C8 rc2 |
| 16 | TZ_COMPLIANCE S03 "MET, tz_verify alpha 0.5" | FAKE at rc1. The vignette shader discarded RGB, so the ember tint never drew. The pulse was normalized against `noise_radius` (10.4) instead of the 0–1 `get_noise_level()` scale, so running gave ratio 0.08. Alpha was capped at the 0.55 base, so nothing pulsed. The check `alpha > 0` passed at zero noise. Fixed: the shader keeps `COLOR.rgb`, the pulse uses the noise-bar scale, and the check measures the frame edge. | C8 rc2 |
| 17 | TZ_COMPLIANCE G12b "cleared by Stability L5" | FAKE at rc1. The code cleared the flicker only at stability bonus ≥ 1.0, but L5 gives 0.5. Now keyed on level == max; tz_verify checks spread 0 at L5 through the real upgrade path. | C8 rc2 |
| 18 | TZ_COMPLIANCE A03 "stealth/walk/run use distinct samples" | FAKE at rc1. Stealth and walk shared one file; asphalt/puddle/glass had one sample at every speed; the probe printed `fails=0` unconditionally. See TZ_DECISIONS DR-A03. | C8 rc2 |
| 19 | TZ_COMPLIANCE G17 "hardcore death deletes save" | Partial at rc1. `.bak2`/`.bak3` survived and the loader falls back to them. All deletes now go through `_remove_with_backups`. | C8 rc2 |
| 20 | TZ_COMPLIANCE G24 and C03 "MET" | G24 closes after D1–D9 FULL, not at first D10 entry: that is DECIDED (DR-2), as TZ_DECISIONS already said. C03 works only on a probe weapon; no gameplay weapon exists (G25), so it is DEFERRED-STRUCTURAL. | C8 rc2 |
| 21 | CORRECTION_LOG #8 cited `c00f118` for the P2m fix; FUNCTION_MATRIX footer "Spine 87 / Extra 27"; TZ_COMPLIANCE "13 checks" | P2m's `_ensure_playing()` landed in `9fc8665`. The matrix has 90 spine rows (58 AL + 32 IN) and 24 X rows. tz_verify had 14 checks at rc1 and 15 at rc2. | C8 rc2 |
| 22 | CORRECTION_LOG #17 / TZ G12b: "checked through the real upgrade path" | Only the purchase path applied flashlight upgrades, so every upgrade (not just L5 Stability) was lost on respawn or Continue. The formulas also used a hard-coded base (energy 1.0, range 8) instead of the scene's 24 / 16 m, so buying Brightness dimmed the light and light_radius shortened it. Fixed: the player re-applies upgrades in `_ready` from the scene base; suite P2r asserts L5 + Brightness survive a real respawn. | C8 rc3 |
| 23 | #9 / TZ_DECISIONS A04: the caps hold because `ambience/` is A02's music layers | `ambience/` also holds about 15 MB of real district, detail and weather ambience, and the dark/lit layers live in `music/`. The caps hold because `ambience/wav_src` (29.9 MB) is excluded from export: about 23.5 MB exported non-music, about 38 MB music. | C8 rc4 |
| 24 | #3: `visual_quality.tres` is the single source for fog | Not at rc3: `fog_setup.gd` set a fixed 0.015 on every main_3d load, after WorldEnvSetup applied the tier. The line is removed; tz_verify checks fog at load against the tier preset (mutation-tested). | C8 rc4 |
| 25 | #22: flashlight upgrades fixed by re-applying on spawn | That made the shared `flashlight_upgrades.cfg` leak into New Game, hardcore wipes and other slots while coins reset. Upgrades are now per-run save data (`"flashlight"` key), cleared by `reset_all()`. Stability L1-L4 did nothing (softness write to a null material). They now cut battery drain 10-50% per GDD §3.3. Suite P2r covers reset and drain (mutation-tested). | C8 rc4 |
| 26 | TZ V02 "No neon / #fff" MET | The monster hit flash set pure white, and its restore duplicated the flash material, so a monster stayed white after its first hit. The flash is now brass and restores the original material; suite P2b is the regression (mutation-tested). | C8 rc4 |
| 27 | ORDER_PASS_REPORT phase and residual lists after C8 round 1 | G24/C03 still listed as "Applied", A03 missing from "Decided", C5 row cited docs-only `c00f118`, visual row showed rc1 numbers. All corrected. | C8 rc4 |
| 28 | Tag `v8.0.0-rc4` on origin pointed at `5cf3b27` (QA guard only) | It was pushed by mistake: a failing docs script did not stop the tag/push commands after it. Deleted from origin within minutes. The real `v8.0.0-rc4` is on the round-3 fix commit. | C8 rc4 |
| 29 | #25: "P2r covers ... drain (mutation-tested)" | At rc4, P2r only read the `_flashlight_drain_cut` field, so deleting the drain line in `_update_battery` still passed. P2r now calls `_update_battery(1.0)` on the respawned player and measures the battery drop (cut 0.5 at L5); deleting the drain line fails it (measured cut 0.0). | C8 rc5 |
| 30 | `5cf3b27`: the guard "only removes files the run itself created" | A lost snapshot dir made restore delete every profile file and still report success; restore now refuses unless the snapshot list and files are intact (`--demo` covers it, mutation-tested). tz_verify and direct suite runs bypassed the shell guard: tz_verify backed up only save files and only after `start_game` had already reset and autosaved. Both runners now take an in-process snapshot (`scripts/tools/_user_data_snapshot.gd`) before anything runs and verify the restore; direct runs left all 11 profile files sha256-identical. | C8 rc5 |
| 31 | ORDER_PASS_REPORT / TZ_COMPLIANCE / ARENA_CLOSURE figures after round 3 | Known-bad frame reads 13.19% (not 9.49%); the V02 frame is hue-only 0.01% (not 0.05%); the candidate line still named rc1; "42 checks" predates the guard gates (commit `b8b20c0` also says 42); the R0 row cited rc2 frame numbers; the tz_verify count was stale; the TZ footer claimed every audit row is in the ledger (only GAP rows are); CORRECTION_LOG said "Newest first". All corrected. | C8 rc5 |
| 32 | #30: the in-process snapshot and the shell guard protect the profile | At rc5, a failed snapshot did not stop the run: `cp -p` in `udg_snapshot` was unchecked, and a null `take()` let tz_verify and the suite keep going, after which restore refused and the profile stayed overwritten. Every runner now aborts before running the game when the snapshot fails (`udg_snapshot` copies and verifies each file). | C8 rc6 |
| 33 | #31 "All corrected"; TZ_DECISIONS S03 reason | ORDER_PASS still credited the rc4 tag with 42 checks; 42 was measured on the rc4 game code before the guard commit, and the tag counts 44. The S03 decision cited 87-98% edge share (measured 85-98%) and leaned on the visual gate as the R0 lock; the real lock is the LUT import pin, since the gate re-measures frames that were clean only because textures never loaded. Both corrected. | C8 rc6 |
| 34 | #30-#33 and `4061f1b`: the guard is safe on every path | At rc6, a failed restore inside the EXIT trap did not change the exit code (a green bot or suite still exited 0), a failed copy left a partial snapshot in temp, `_udg_demo` had an unchecked `mktemp`, and the demo never exercised a failed copy. tz_verify had no crash-safe restore. Fixed: the trap is `udg_restore || exit 97`, a failed copy removes its partial snapshot, the demo covers the failed copy (mutation-tested) and keeps all its temp inside its own dir, and `tools/qa_sim/tz_verify` runs the probe under the guard. The S03 edge share is 70-98% (not 85-98%), the report no longer calls the frame gate the R0 lock, and ARENA B7 records its deferred legacy-achievements half. | C8 rc7 |
| 35 | #34: `tools/qa_sim/tz_verify` makes tz_verify crash-safe; S03 hits "at hue 320-330"; B7 deferral listed | The probe still documented a direct godot launch that had no crash-safe restore, and the wrapper was committed without its executable bit. The runner now refuses to start unless the wrapper launched it (`TLS_UDG_GUARDED`; a direct launch exits 2 before touching anything), and the wrapper is 100755. Only 18-27% of the S03 hits fall in 320-330 degrees; the decision now cites the 260-345 gate band with median hues 319-335. The B7 deferred half is now in the ARENA open-defers list and the report's residual table. | C8 rc8 |
| 36 | ORDER_PASS round-7 row: engine battery not needed because no engine gate loads the tz_verify files | False: the compile gate (`_compile_gate.gd`) loads every `.gd`. Re-run on the rc8 code: check.sh full 44 green. Also: TZ_DECISIONS G18/G19 cited GDD.md:167-178 (the table is 168-181), and the report's residual table was missing the deferred R-02 speed watchdog. | C8 rc9 |
| 37 | TZ V05 MET; TZ_DECISIONS G28/D04 and I02 reasons; FUNCTION_MATRIX legend | V05 is 2048 on desktop only; mobile uses 1024 (`project.godot` `.mobile` override), now DECIDED with its own row. G28/D04 cited `GDD.md:3-6` for a specific-over-general rule that is not there (those lines only rank the GDD above its appendices). I02 said 1291 keys; the count is 1301. FIXED and PARTIAL were used in the matrix but not defined. All corrected. | C8 rc10 |
| 38 | `5cf3b27`..`9936ab3`: QA runs no longer touch the owner's profile unguarded; V05-mobile DR-3; FIXED rows name their commit | The windowed perf and audio probes (documented as direct `godot --windowed` launches) start a New Game with no snapshot. They now refuse unless a guarded shell set `TLS_UDG_GUARDED`, and `tools/qa_sim/guarded_windowed` is the documented launcher. V05-mobile claimed DR-3 without any measurement; the override is removed so mobile follows the GDD's 2048. The FIXED rows now cite `0d3d533`, `0f9685a`, `fe3007a`. D1 re-measured at 253 draw calls (was 246). | C8 rc11 |
| 39 | #38 "the documented launcher" guards QA runs; TZ_DECISIONS DR labels; S02 reason; FUNCTION_MATRIX X12/X24 | #38 only covered perf/audio: capture_stills, boot_check, gameplay_shot, ShotTool, attack_sim, game_test_3d and direct bot/suite runs still ran unguarded, and a killed suite skipped its in-process restore. Root fix: the first autoload, `QaLaunchGuard`, snapshots the profile on every `scenes/tools/*` or `--shot` launch not wrapped by the shell guard, restores it at exit, and keeps a crash copy that the next unguarded launch restores (a killed run was recovered byte-identical). It replaces the round-7/10 refusals and `_user_data_snapshot.gd`. DR labels: S03 is a note (MET), V05-mobile is DR-4, A03 is DR-5, G22 is DR-6 (owner decision, PLAN.md Этап 1), D03 and E05 were not measured rejections, so DR-4 was applied (GDD stage lighting; district reward curve). G34 audio logs exist and are counted in the documents total. S02: the low_profile skill does shorten monster sight. X12 is now verified by suite P2r; X24 had stale draw-call text. | C8 rc12 |
